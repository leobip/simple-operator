---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: simples.demo.demo.local
spec:
  group: demo.demo.local
  names:
    kind: Simple
    listKind: SimpleList
    plural: simples
    singular: simple
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Simple is the Schema for the simples API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Simple
            properties:
              message:
                description: Message is the string to print
                minLength: 1
                type: string
            required:
            - message
            type: object
          status:
            description: status defines the observed state of Simple
            properties:
              replied:
                description: Replied indicates that we’ve seen and logged the Message
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
